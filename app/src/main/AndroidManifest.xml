<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IF_shop_android_app"
        tools:targetApi="31">
        <activity
            android:name=".app.activities.AddItemActivity"
            android:exported="false" />
        <activity
            android:name=".app.activities.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".app.activities.WeatherActivity"
            android:exported="false" />
        <activity
            android:name=".app.activities.ListActivity"
            android:exported="false" />
        <activity
            android:name=".app.activities.AdminActivity"
            android:exported="false" />
        <activity
            android:name=".app.activities.ForgotPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".app.activities.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".app.activities.MainActivity"
            android:exported="false" />
        <activity
            android:name=".app.activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>